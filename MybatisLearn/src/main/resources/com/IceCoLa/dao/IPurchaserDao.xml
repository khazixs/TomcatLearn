<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dte/mybatis-3-mapper.dtd">
<mapper namespace="com.IceCoLa.dao.IPurchaserDao">
    <cache readOnly="true"/>
    <select id="selectAll" resultType="com.IceCoLa.domain.Purchaser">
        select pur.id id,pur.name name,pur.address address,pro.id `product.id`,pro.name `product.name`,pro.price `product.price`
       from purchaser pur left join product pro on pro.purchaserId = pur.id;
    </select>
</mapper>